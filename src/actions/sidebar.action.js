import { TOGGLE_SIDEBAR, HIDE_SIDEBAR, SHOW_SIDEBAR } from '../types'

const toggleSidebar = {
  type: TOGGLE_SIDEBAR
}

const hideSidebar = {
  type: HIDE_SIDEBAR
}

const showSidebar = {
  type: SHOW_SIDEBAR
}

export {
  toggleSidebar,
  hideSidebar,
  showSidebar
}
